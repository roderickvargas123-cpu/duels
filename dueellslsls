local randomSig = "Sys_" .. tostring(math.random(10000, 99999))
if getgenv()[randomSig] then return end
getgenv()[randomSig] = true

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local VirtualInputManager = game:GetService("VirtualInputManager")

local LocalPlayer = Players.LocalPlayer
local Mouse = LocalPlayer:GetMouse()

local holdingTriggerButton = false
local ejecutandoMacro = false

-- ==========================================
-- CONTROLES (Modo Headless - Sin GUI)
-- ==========================================
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if not gameProcessed and input.KeyCode == Enum.KeyCode.P then
        holdingTriggerButton = true
    end
end)

UserInputService.InputEnded:Connect(function(input, gameProcessed)
    if input.KeyCode == Enum.KeyCode.P then
        holdingTriggerButton = false
    end
end)

-- ==========================================
-- MACRO DE DISPARO (Bypass de Tiempos)
-- ==========================================
local function GhostShoot()
    if ejecutandoMacro then return end
    ejecutandoMacro = true

    -- 1. Saca el arma
    VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.Two, false, game)
    VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.Two, false, game)
    
    -- Tiempo de estabilización
    task.wait(0.06) 
    
    -- 2. Dispara
    if mouse1click then
        pcall(mouse1click) -- pcall evita que el AC intercepte errores
    else
        VirtualInputManager:SendMouseButtonEvent(0, 0, 0, true, game, 0)
        VirtualInputManager:SendMouseButtonEvent(0, 0, 0, false, game, 0)
    end
    
    -- Cooldown humanoide
    task.wait(0.5)
    ejecutandoMacro = false
end

-- ==========================================
-- MOTOR DE PROXIMIDAD 3D (Indetectable por barridos)
-- ==========================================
-- Este número define qué tan lejos de la hitbox real puede estar tu mira para disparar.
-- 1.5 a 2.0 studs equivale a la ventaja del Hitbox Expander sin alterar físicas.
local DISTANCIA_DETECCION = 1.8 

RunService.RenderStepped:Connect(function()
    if holdingTriggerButton and getgenv()[randomSig] and LocalPlayer.Character then
        
        local targetFound = false
        
        -- Verificamos si el ratón está apuntando a algo en el mundo 3D
        if Mouse.Hit then
            local aimPosition = Mouse.Hit.Position
            
            for _, player in ipairs(Players:GetPlayers()) do
                if player ~= LocalPlayer and player.Character then
                    local hrp = player.Character:FindFirstChild("HumanoidRootPart")
                    local humanoid = player.Character:FindFirstChild("Humanoid")
                    
                    if hrp and humanoid and humanoid.Health > 0 then
                        -- ==========================================
                        -- MAGIA MATEMÁTICA: Medimos la distancia
                        -- ==========================================
                        local distancia = (hrp.Position - aimPosition).Magnitude
                        
                        -- Si tu mira pasa "cerca" del torso según nuestro radio invisible
                        if distancia <= DISTANCIA_DETECCION then
                            targetFound = true
                            break
                        end
                    end
                end
            end
        end
        
        if targetFound then
            GhostShoot()
        end
    end
end)

-- Notificación nativa de Roblox para confirmar la inyección (El AC no detecta esto como cheat)
pcall(function()
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "Rendimiento",
        Text = "Optimización de red aplicada.",
        Duration = 3
    })
end)
